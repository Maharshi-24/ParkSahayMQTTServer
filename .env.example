# Server configuration
# Render will automatically assign a PORT, but you can use this for local development
PORT=3000

# MQTT Broker configuration
# TEMPORARY VALUE: Use this public test broker until your own broker is set up
# This is the HiveMQ public test broker - DO NOT use for production or send sensitive data
MQTT_BROKER_URL=mqtt://broker.hivemq.com

# Authentication - Leave blank if using the public test broker
# Add your actual credentials once your own broker is set up
MQTT_USERNAME=
MQTT_PASSWORD=

# IMPORTANT: Replace the broker URL with your own MQTT broker's address
# once it's set up. Format: mqtt://your-broker-ip-or-domain
# Examples:
# MQTT_BROKER_URL=mqtt://203.0.113.10
# MQTT_BROKER_URL=mqtt://broker.example.com
